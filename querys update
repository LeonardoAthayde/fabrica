ALTER TABLE  `fluxograma_acoes_tempo` ADD  `tempo_marcado` TIME NOT NULL;
ALTER TABLE  `fluxograma_acoes_tempo` ADD  `tempo_ajustado` TIME NOT NULL;

CREATE TABLE IF NOT EXISTS `referencia_rendimento_uniao_assn` (
  `referencia_id` int(11) NOT NULL,
  `referencia_rendimento_id` int(11) NOT NULL,
  KEY `referencia_id` (`referencia_id`),
  KEY `referencia_rendimento_id` (`referencia_rendimento_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;


ALTER TABLE `referencia_rendimento_uniao_assn`
  ADD CONSTRAINT `referencia_rendimento_uniao_assn_ibfk_2` FOREIGN KEY (`referencia_rendimento_id`) REFERENCES `referencia_rendimento` (`id`) ON DELETE CASCADE ON UPDATE CASCADE,
  ADD CONSTRAINT `referencia_rendimento_uniao_assn_ibfk_1` FOREIGN KEY (`referencia_id`) REFERENCES `referencia` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;



ALTER TABLE  `referencia_rendimento` CHANGE  `molde_id`  `molde_id` INT( 11 ) NULL ,
CHANGE  `referencia_id`  `referencia_id` INT( 11 ) NULL ,
CHANGE  `comprimento`  `comprimento` FLOAT NOT NULL DEFAULT  '-1',
CHANGE  `pecas`  `pecas` INT( 11 ) NOT NULL DEFAULT  '-1',
CHANGE  `peso`  `peso` FLOAT NOT NULL DEFAULT  '-1',
CHANGE  `preco`  `preco` FLOAT NOT NULL DEFAULT  '-1',
CHANGE  `tecido_id`  `tecido_id` INT( 11 ) NULL

ALTER TABLE  `referencia_rendimento_uniao_assn` ADD UNIQUE (
`referencia_id` ,
`referencia_rendimento_id`
);


ALTER TABLE  `referencia_rendimento` DROP FOREIGN KEY  `referencia_rendimento_ibfk_3` ;


deletar campo "referencia_id" da tabela referencia_rendimento